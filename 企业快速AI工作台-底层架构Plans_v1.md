# 企业快速AI工作台 · 底层架构 Plans

---

## 〇、项目概览

**一句话定位**：给企业一套"解压即用"的 AI 工作台——**oharness.exe** 是由 NW.js 封装的企业客户端（Odoo 绿色版），把 **Odoo 业务 + AI Agent + 内网穿透 + 本地打印 + 客户机配置** 全部装进一个绿色一键环境，企业客户下载即用、可视化配置、一键启停。

**核心理念**：
- **绿色免安装**：oharness 拿到即用，不需要任何手动环境配置，延续"下载就能用"的产品形态；
- **AI 内置**：工作台原生集成 AI Agent（暂定 deepseek harness），业务系统里直接用 AI 能力；
- **云端直连**：oharness 通过 `/v1/xxx` 对接云端 Odoo 服务器（暂定 odooapp.cn / wic.odooapp.cn），信息交互与远程操作；
- **内网穿透**：内置 frp 客户端打通客户机对外访问，替代传统 VPN，更轻、更安全、可一键自动化配置。

> 说明：oharness 与 NW 主程序之间的本地通讯（`/odoo.app`）已由单独模块承载，**不在本 plans 范围内展开**。

> **目录约定**：项目的**规划与进度**统一存放在仓库 `./plans/` 子目录下（含本文件及其后续进度记录），保持规划与执行进度集中、可追踪。

---

## 一、技术目标

### 1. Server 服务端功能（云端 Odoo 服务器）
服务端功能集中在云端 Odoo 服务器侧，负责服务端能力与对客户端的下发/通讯，另有项目规划：

1. **主要集中在模块 `ai_saas_srv`**：服务端核心能力落在 `ai_saas_srv` 模块；
2. **发送客户端配置 `/odoo.app`**：服务端通过 `/odoo.app` 通道向客户端下发配置；
3. **与客户端通讯 `/v1/xxx`**：服务端与客户端以 `/v1/xxx` REST API 通讯。

> 云端服务器暂定 `odooapp.cn` / `wic.odooapp.cn`。

### 2. Client 客户端功能（oharness · Odoo 绿色版）

#### 2.1 用 NW.js 封装 oharness（企业客户端 · Odoo 绿色版）
oharness.exe 由一个 **NW.js 主程序**统一封装和管理，内含并一键管理：

| 组件 | 说明 |
|---|---|
| **odoo 环境** | Python / 依赖 / addons，随包携带 |
| **odoo 本地打印** | 本地打印服务，一键配置打印机 |
| **AI Agent 客户端** | 暂定 deepseek harness，内置 AI 能力 |
| **frp 内网穿透客户端** | 客户机对外访问通道，自动配置 |
| **客户机 PC 配置** | hosts、端口、自启动等新手级一键配置 |

> 本质：**一个 oharness.exe = 完整的企业 AI 工作台**，所有子组件由 NW 主程序统一调度、可视化配置、一键启停。

#### 2.2 本地通讯：NW.js 与 oharness 之间
- NW.js 主程序与 oharness 的本地通讯，以 **`/odoo.app`** 通道承载（**该模块已独立，不在本 plans 展开**）。

#### 2.3 云端通讯：NW.js 及 oharness 与云端 Odoo 服务器通过 `/v1/xxx` 交互
- NW.js 主程序、oharness 与云端 Odoo 服务器以 **`/v1/xxx` REST API** 通讯；
- 能力：**信息交互 + 远程操作**（如授权、激活、版本同步、远程配置下发、AI 服务调用）。

---

## 二、整体架构分层

```
┌────────────────────────────────────────────────────────────┐
│                   企业客户机（Windows / 后续 Mac）             │
│                                                              │
│   ┌────────────────────────────────────────────────────┐    │
│   │          oharness.exe  (NW.js 封装的绿色客户端)      │    │
│   │  可视化配置面板 · 一键启停 · 状态看板 · 日志输出        │    │
│   │                                                    │    │
│   │  ┌─────────┬──────────┬─────────┬───────────────┐  │    │
│   │  │ odoo环境 │ 本地打印  │AI Agent │   frpc 穿透    │  │    │
│   │  │ addons   │          │deepseek │  自动配置启停   │  │    │
│   │  │ 业务数据  │          │ harness │               │  │    │
│   │  └─────────┴──────────┴─────────┴───────────────┘  │    │
│   │                                                    │    │
│   │  客户机PC配置：hosts · 端口检测 · 开机自启 · 浏览器   │    │
│   └─────────────────────┬──────────────────────────────┘    │
└─────────────────────────┼────────────────────────────────────┘
                          │  云端 /v1/xxx
┌─────────────────────────▼────────────────────────────────────┐
│              云端 Odoo 服务器                                 │
│   odooapp.cn / wic.odooapp.cn                                │
│   · 云端业务 /v1/xxx API  · 授权激活  · 远程配置  · AI 服务    │
└──────────────────────────────────────────────────────────────┘

公网 frps 服务器（frp 服务端，一次配好，负责中转客户机访问）
```

**结构说明**：
1. **oharness（NW.js 封装）**：单文件/单文件夹绿色客户端，内部统一调度 odoo、AI、frpc、打印、客户机配置；
2. **本地通讯 `(/odoo.app)`**：独立模块承载，不在本 plans 展开；
3. **云端 `/v1/xxx`**：提供业务、授权、远程配置与 AI 能力。

---

## 三、模块清单（oharness 内建）

### 3.1 业务系统模块
- **odoo 环境**：启动 / 停止 / 重启 / 状态检测
- **odoo 本地打印**：打印机选择、打印服务启停、测试打印
- **addons 路径管理**

### 3.2 AI 能力模块
- **AI Agent 客户端（deepseek harness）**：启动 / 配置 API Key / 对话测试 / 接入 odoo 业务

### 3.3 网络穿透模块
- **frp 客户端（frpc）**：自动生成 `frpc.ini`（server 地址 / token / 端口映射）、一键启停、连通性检测、开机自启

### 3.4 客户机配置模块
- **hosts 一键修改**（Windows 提权 / Mac sudo）
- **端口检测**（80 / 8069 / 5432 等）
- **开机自启**（Windows 注册表 / Mac launchd）
- **一键打开浏览器**访问工作台

### 3.5 云端对接模块
- **云端登录 / 授权激活**（对接 `/v1/xxx`）
- **版本检查与远程配置下发**
- **AI 服务远程调用**

---

## 四、通讯协议设计

| 通道 | 形态 | 用途 | 备注 |
|---|---|---|---|
| **本地 `(/odoo.app)`** | NW 主程序 ↔ oharness | 配置本机、下发指令、读状态 | **独立模块承载，本 plans 不展开** |
| **云端 `/v1/xxx`** | NW + oharness ↔ 云端 Odoo | 信息交互、远程操作、授权、AI 服务 | REST API，JSON |
| **frp 隧道** | 客户机 ↔ frps ↔ 外部 | 客户机对外访问 | 仅暴露指定端口 |

---

## 五、跨平台与部署形态

- **当前**：Windows（绿色免安装，`oharness.exe`）
- **后续**：Mac（同一套 NW 源码，换平台二进制；frp 用 darwin 版；hosts/自启动走 Mac 机制）
- **形态**：整个包放在一个文件夹，客户解压即用，**无需安装程序**

---

## 六、自动化配置能力（新手级）

1. **一键改 hosts**（自动提权）
2. **自动生成 frpc.ini** + 一键启停 frpc
3. **表单化配置** odoo.conf / nginx.conf（免手动改文件）
4. **端口占用检测** + 冲突提示
5. **连通性自检**：隧道、本地服务、云端 API 三路自检，界面直读
6. **开机自启**：登录自动拉起核心服务
7. **日志实时输出**到界面，新手能直接看到报错

---

## 七、安全设计

- **frp 替代 VPN**：只暴露指定端口，攻击面小；token 认证
- **云端 API**：HTTPS + 鉴权（token / 签名）
- **本地通讯 `(/odoo.app)`**：仅监听 localhost，不对外（由独立模块落实）
- **权限最小化**：仅 hosts/注册表/launchd 需要提权
- **杀软提示**：打包签名，降低修改 hosts / 启停 exe 的拦截概率

---

## 八、里程碑

| 阶段 | 内容 | 交付 |
|---|---|---|
| **M1 骨架** | NW.js 工程 + 基础面板 + 启停 odoo 进程 | 可运行的配置器壳 |
| **M2 本地配置** | hosts、端口检测、日志、本地配置闭环 | 本地全流程可用 |
| **M3 AI 模块** | deepseek harness 接入、API 配置、对话测试 | 内置 AI 可用 |
| **M4 穿透** | frpc 集成、自动配置、连通性检测 | 外网可访问 |
| **M5 云端** | `/v1/xxx` 对接、授权激活、远程配置 | 云端闭环 |
| **M6 跨平台** | Mac 适配、打包、签名 | Win + Mac 双端交付 |

---

## 九、风险与备选

| 风险 | 应对 |
|---|---|
| 修改 hosts 被权限/杀软拦截 | 提权启动 + 打包签名 + 清晰提示 |
| 绿色包路径不固定 | 全程相对路径，不写死 C 盘 |
| NW 生态维护 | 备选 Electron（IPC 更繁琐），当前优先 NW |
| 云端 API 设计变动 | 预留 `/v1/xxx` 接口版本化，接口先行定义 |
| frp 服务器依赖 | 需一台公网 frps，纳入部署清单 |